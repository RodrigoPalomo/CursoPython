#

# ! INSTALACIÓN DE PAQUETES (pip install) y (PyPi)

# para instalar un paquete en python = pip install [nombre]

# para instalar una versión específica = pip install [nombre]==[versión]. Ej -> pip install django==2.2